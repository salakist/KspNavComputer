{
  "name": "kspnavcomputer-scripts",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^19.8.1",
    "@commitlint/config-conventional": "^19.8.1"
  },
  "scripts": {
    "lint:commitmsg": "commitlint --config ./commitlint.config.cjs --edit"
  }
}
